// import * as dotenv from "dotenv";

// console.log(process.env);
// dotenv.config();
// require("dotenv").config();

// const options = {
//     oauth_consumer_key: `${process.env.OAUTH_CONSUMER_KEY}`,
//     oauth_nonce: `${process.env.OAUTH_NONCE}`,
//     oauth_signature: `${process.env.OAUTH_SIGNATURE}`,
//     oauth_signature_method: "PLAINTEXT",
//     oauth_timestamp: `${process.env.OAUTH_TIMESTAMP}`,
//     oauth_token: `${process.env.OAUTH_TOKEN}`,
//     oauth_verifier: `${process.env.OAUTH_VERIFIER}`,
//     oauth_version: "1.0",
// };
// const method = "GET";

// fetch("https://api.discogs.com/database/search", options, method)
//     .then((response) => response.json())
//     .then((response) => console.log(response))
//     .catch((err) => console.error(err));
